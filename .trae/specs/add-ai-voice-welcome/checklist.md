- [x] 成功引入 `public/welcome.mp3` 并在应用中可被加载。
- [x] 成功配置全局 Zustand store 管理音频播放状态和播放历史。
- [x] 从底部导航栏点击 AI 按钮进入页面时，能正常触发语音播放（规避浏览器自动播放限制）。
- [x] 语音播放期间，AI 页面有明显的动效与文字反馈（如：“正在说话...”）。
- [x] 在同一会话中（不刷新浏览器），再次进入 AI 页面不会重复播放欢迎语音。
- [x] 语音播放期间切换到其他页面，语音会自动停止。
- [x] 在不具备用户交互手势的情况下（如强制刷新当前页面），页面能提供手动的兜底播放入口。
- [x] 运行 `npm run build` 编译通过，没有 TypeScript 或构建错误。